language: python
python:
  - "2.7"
cache: pip
install:
  - pip install python-coveralls
  - pip install coverage
  - pip install nose
script:
  - python setup.py install
  - nosetests --with-coverage
after_success:
  - coveralls #send report to coveralls
